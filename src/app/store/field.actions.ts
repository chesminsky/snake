import { createAction } from '@ngrx/store';

export const randomEgg = createAction('[Field] random egg');
export const changeDirection = createAction('[Field] change direction');
export const tick = createAction('[Field] tick');
